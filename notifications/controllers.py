# notifications/controllers.py
def show_notification(message):
    # Customize this function to handle the notification (e.g., mark as read, store in a log, etc.)
    print(f"Notification displayed: {message}")
