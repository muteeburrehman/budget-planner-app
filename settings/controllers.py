# settings/controllers.py
def save_settings(categories, budget_limit):
    # Customize this function to save user settings (e.g., categories, budget limit)
    print(f"Settings saved - Categories: {categories}, Budget Limit: {budget_limit}")
